# Copy file này thành .env và điền IP LAN của máy bạn (chạy `ipconfig` để xem)
# Android thật: dùng IP LAN của máy tính
# Android Emulator: dùng 10.0.2.2
# iOS Simulator: dùng localhost
EXPO_PUBLIC_API_BASE_URL=http://<YOUR_LAN_IP>:3000/api
