// Auth types are now in ~/types/index.ts
// Re-export for backwards compatibility
export { UserRole, AuthStatus } from '../../types';
export type { User, AuthPayload, UserRoleValue, AuthStatusValue } from '../../types';
